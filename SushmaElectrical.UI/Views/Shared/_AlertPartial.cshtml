@*  @if (TempData["success"] != null)
{
    <div id="successAlert" class="alert alert-success alert-dismissible fade show" role="alert">
        <strong>Success!</strong> @TempData["success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
@if (TempData["warning"] != null)
{
    <div id="warningAlert" class="alert alert-warning alert-dismissible fade show" role="alert">
        <strong>Warning!</strong> @TempData["warning"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

@if (TempData["error"] != null)
{
    <div id="errorAlert" class="alert alert-danger alert-dismissible fade show" role="alert">
        <strong>Error!</strong> @TempData["error"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
@if (TempData["info"] != null)
{
    <div id="infoAlert" class="alert alert-info alert-dismissible fade show" role="alert">
        <strong>Info!</strong> @TempData["info"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<script>
    // Add fade-out effect to alerts after 3 seconds
    window.setTimeout(function () {
        $("#successAlert").fadeTo(500, 0).slideUp(500, function () {
            $(this).remove();
        });
        $("#warningAlert").fadeTo(500, 0).slideUp(500, function () {
            $(this).remove();
        });
        $("#errorAlert").fadeTo(500, 0).slideUp(500, function () {
            $(this).remove();
        });
        $("#infoAlert").fadeTo(500, 0).slideUp(500, function () {
            $(this).remove();
        });
    }, 3000);
</script>


 *@
@*  <style>
    .floating-alert {
        position: fixed;
        top: 20px; /* Adjust this value to position the alert vertically */
        right: 20px; /* Adjust this value to position the alert horizontally */
        z-index: 1000; /* Ensure the alert is above other content */
        width: 300px; /* Adjust this value to control the width of the alert */
    }

 </style> *@

@if (TempData["success"] != null)
{
    <div id="successAlert" class="floating-alert alert alert-success alert-dismissible fade show" role="alert">
        <strong>Success!</strong> @TempData["success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
@if (TempData["warning"] != null)
{
    <div id="warningAlert" class="floating-alert alert alert-warning alert-dismissible fade show" role="alert">
        <strong>Warning!</strong> @TempData["warning"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

@if (TempData["error"] != null)
{
    <div id="errorAlert" class="floating-alert alert alert-danger alert-dismissible fade show" role="alert">
        <strong>Error!</strong> @TempData["error"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
@if (TempData["info"] != null)
{
    <div id="infoAlert" class="floating-alert alert alert-info alert-dismissible fade show" role="alert">
        <strong>Info!</strong> @TempData["info"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<script>
    // Add fade-out effect to alerts after 3 seconds
    window.setTimeout(function () {
        $(".floating-alert").fadeTo(500, 0).slideUp(500, function () {
            $(this).remove();
        });
    }, 3000);
</script>

